import React from 'react'

const Headcomponent = ({first,second}) => {
  return (
    <div>
        <h1 className='text-center signup-heading'>{first} {second}</h1>
    </div>
  )
}

export default Headcomponent